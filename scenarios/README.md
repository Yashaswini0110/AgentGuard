# Scenario fixtures

Small JSON descriptors for **additional demo domains** (loan, triage, …). They document intent only until a dedicated adapter is implemented. The active reference adapter remains `hiring_demo_v1` exercised from Scenario Lab.
