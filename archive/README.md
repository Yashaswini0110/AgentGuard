# Archive

- **`streamlit_dashboard.py`** — Former Streamlit UI. The **canonical product UI** is the React app under `ui/app/`. Run `npm run dev` from `ui/app` and start the FastAPI server from the project root. This file is kept for reference only.
